const prompt = require("prompt-sync")();

console.log(`\n\tAtividade C1!!\n`);

let numeroA = Number (prompt("\t\tDigite o numero A (inteiro): "));
let numeroB = Number (prompt("\t\tDigite o numero B (inteiro): "));

console.log(`\n\t\tA soma entre eles é ${numeroA+numeroB}`);